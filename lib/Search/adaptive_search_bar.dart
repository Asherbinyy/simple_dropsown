import 'package:flutter/material.dart';

class AdaptiveSearchBar extends StatelessWidget {
  const AdaptiveSearchBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
